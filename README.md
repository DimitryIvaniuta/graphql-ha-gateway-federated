# graphql-ha-gateway-federated
High-availability GraphQL API gateway (Spring Boot, Java) that composes multiple domain services. Schema-first, resilient, with persisted queries + API keys in PostgreSQL (Flyway/JPA). Includes stubs for Order/Inventory/Payment services and shared modules. Ready for HA deployment behind a load balancer
